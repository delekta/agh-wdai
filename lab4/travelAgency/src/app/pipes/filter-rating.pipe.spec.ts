import { FilterRatingPipe } from './filter-rating.pipe';

describe('FilterRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
